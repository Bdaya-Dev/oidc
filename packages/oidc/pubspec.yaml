name: oidc
description: A comprehensive OpenIdConnect package
version: 0.1.1
repository: https://github.com/Bdaya-Dev/oidc/tree/main/packages/oidc

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.7.0"
flutter:
  plugin:
    platforms:
      android:
        default_package: oidc_android
      ios:
        default_package: oidc_ios
      linux:
        default_package: oidc_linux
      macos:
        default_package: oidc_macos
      web:
        default_package: oidc_web
      windows:
        default_package: oidc_windows

dependencies:
  flutter:
    sdk: flutter
  # Endorsed packages
  oidc_android: ^0.1.0+1
  oidc_core: ^0.2.0
  oidc_ios: ^0.1.0+1
  oidc_linux: ^0.1.0+1
  oidc_macos: ^0.1.0+1
  oidc_platform_interface: ^0.1.0+1
  oidc_web: ^0.1.0+1
  oidc_windows: ^0.1.0+1
  # ====================
  http: ^1.1.0
  jose: ^0.3.4
  retry: ^3.1.2
  rxdart: ^0.27.7
  logging: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  plugin_platform_interface: ^2.0.0
  very_good_analysis: ^5.0.0
