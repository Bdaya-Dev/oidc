name: oidc_desktop
description: Base Implementation for oidc_* desktop plugins (mainly windows/linux)
version: 0.1.1
repository: https://github.com/Bdaya-Dev/oidc/tree/main/packages/oidc_flutter_appauth

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
  oidc_core: ^0.3.2
  oidc_loopback_listener: ^0.1.2
  oidc_platform_interface: ^0.2.1
  url_launcher: ^6.1.14
  window_to_front: ^0.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^5.0.0
